<%
  oauth_account = local_assigns[:oauth_account] || current_user
  season = local_assigns[:season] || Season.current_or_latest_number
%>

<% content_for(:trend_tabs) do %>
  <a href="<%= all_seasons_stats_path(oauth_account) %>" class="UnderlineNav-item js-tab <%= 'selected' if is_all_seasons_stats_page? %>" role="tab">
    All seasons
  </a>

  <% if oauth_accounts.size > 1 %>
    <a href="<%= all_accounts_stats_path(season) %>" class="UnderlineNav-item js-tab <%= 'selected' if is_all_accounts_stats_page? %>" role="tab">
      All accounts
    </a>
  <% end %>
<% end %>
