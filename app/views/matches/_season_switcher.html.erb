<div class="dropdown js-menu-container d-inline-block">
  <button class="btn-link no-underline text-white p-0 dropdown-toggle js-menu-target" type="button" aria-expanded="false" aria-haspopup="true">
    <% if selected_season == :all %>
      All seasons
    <% else %>
      Season <%= selected_season %>
    <% end %>
    <div class="dropdown-caret"></div>
  </button>

  <div class="dropdown-menu-content js-menu-content">
    <ul class="dropdown-menu dropdown-menu-se">
      <% if params[:battletag] %>
        <li>
          <a class="dropdown-item <%= 'text-bold' if selected_season == :all %>" href="<%= all_seasons_stats_path(params[:battletag]) %>">
            All seasons
          </a>
        </li>
      <% else %>
        <li>
          <a class="dropdown-item <%= 'text-bold' if selected_season == :all %>" href="<%= stats_path %>">
            All seasons
          </a>
        </li>
      <% end %>
      <% season_options.each do |label, season| %>
        <li>
          <a class="dropdown-item <%= 'text-bold' if season == selected_season %>" href="<%= season_switcher_season_path(season) %>">
            <%= label %>
          </a>
        </li>
      <% end %>
    </ul>
  </div>
</div>
